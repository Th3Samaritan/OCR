fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27
pydantic>=2
sqlalchemy>=2.0
pymupdf>=1.24
openpyxl>=3.1        # tables → Excel export (apps/api/tables.py)
pillow>=10.0         # image EXIF + ELA tamper checks (apps/api/integrity.py)

# Extraction provider (real path) — Gemini by default:
google-genai>=0.3
# anthropic>=0.40     # only if EXTRACTION_PROVIDER=anthropic
# deepseek>=0.4.0   # only if EXTRACTION_PROVIDER=deepseek
# Postgres driver (production): set DATABASE_URL=postgresql+psycopg://...
# psycopg[binary]>=3.1
